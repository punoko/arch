name: CI
on:
  push:
    branches: [ main ]
jobs:
  container-test-job:
    runs-on: ubuntu-latest
    container: archlinux
    steps:
      - name: Check for dockerenv file
        run: (pacman -Syyu && date)
