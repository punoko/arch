name: Test
on:
  push:
    branches: [ main ]
jobs:
  neofetch:
    runs-on: ubuntu-latest
    container:
      image: archlinux
      volumes:
        - /dev:/dev
    steps:
      - run: |
          losetup -f
